No documentation yet but basics:

To run: 'cargo run'

Web pages:

Address: 127.0.0.1:3030 (can change in main.rs)
/ -> Home page, lists posts with newest first
/new-post -> New post (and comment)
/post/{id} -> A certain post with that id, and its comments


TODO:
- Add search
- Make it look less garbage
- Possibly allow non-unique titles
